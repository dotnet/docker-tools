parameters:
  condition: true

steps:
  ################################################################################
  # Cleanup Docker Resources
  ################################################################################
- powershell: $(engDockerToolsPath)/Invoke-CleanupDocker.ps1
  displayName: Cleanup Docker Images
  condition: and(always(), ${{ parameters.condition }})
  continueOnError: true
- powershell: |
    if (Test-Path $(Build.BinariesDirectory)\.imagebuilder) {
      Remove-Item $(Build.BinariesDirectory)\.imagebuilder -Force -Recurse;
    }
  displayName: Cleanup Image Builder
  condition: and(always(), ${{ parameters.condition }})
  continueOnError: true
