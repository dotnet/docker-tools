parameters:
  name: null
  pool: {}
  pushImages: false

jobs:
- job: ${{ parameters.name }}
  pool: ${{ parameters.pool }}
  variables:
    ${{ if eq(parameters.pushImages, 'true') }}:
      buildParameters: -DockerRepo $(acr.server)/public/dotnet-buildtools/image-builder -PushImages
    ${{ if eq(parameters.pushImages, 'false') }}:
      buildParameters: ""
  workspace:
    clean: all
  steps:
    - ${{ if eq(parameters.pushImages, 'true') }}:
      - script: docker login -u $(acr.userName) -p $(BotAccount-dotnet-docker-acr-bot-password) $(acr.server)
        displayName: Docker Login
    - powershell: >
        Microsoft.DotNet.ImageBuilder/build.ps1 $(buildParameters)
      displayName: Build and Publish ImageBuilder
    - ${{ if eq(parameters.pushImages, 'true') }}:
      - script: docker logout
        displayName: Docker Logout
        condition: always()
        continueOnError: true
